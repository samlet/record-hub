# Phase 6 Batch 3：P6-302 Bids tender approval

P6-302 固化招标准备发布审批的安全元数据、Conductor task completion、审批 generation/expected
version 和 drain/fallback 边界。当前只做 Bids contract/worker 静态盘点，缺 native Conductor live
evidence，状态为 `BLOCKED_BY_LIVE_CONNECTOR`。

入口是 `make p6-302`，结果见 [`phase-6-bids-approval.json`](phase-6-bids-approval.json)。
